version: "3"

volumes:
  napse_dtk_local_postgres_data: {}
  napse_dtk_local_postgres_data_backups: {}

services:
  django: &django
    build:
      context: ..
      dockerfile: ./compose/local/django/Dockerfile
    image: napse_dtk_local_django
    container_name: napse_dtk_local_django
    platform: linux/x86_64
    depends_on:
      - postgres
      - redis
    volumes:
      - ..:/app:z
    env_file:
      - ../.envs/.local/.django
      - ../.envs/.local/.postgres
    command: /start
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.django.rule=Host(`localhost`)"
      - "traefik.http.routers.django.entrypoints=web"
    expose:
      - "8000"

  traefik:
    image: "traefik:v2.9.5"
    container_name: napse_dtk_traefik
    command:
      - "--log.level=DEBUG"
      - "--api.insecure=true"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:8000"
      - "--entrypoints.frontend.address=:3005"
      - "--entrypoints.flower.address=:5555"
    ports:
      - "5555:5555"
      - "8000:8000"
      - "8080:8080"
      - "3005:3005"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock:ro"

  postgres:
    build:
      context: ..
      dockerfile: ./compose/server/postgres/Dockerfile
    image: napse_dtk_local_postgres
    container_name: napse_dtk_local_postgres
    volumes:
      - napse_dtk_local_postgres_data:/var/lib/postgresql/data:Z
      - napse_dtk_local_postgres_data_backups:/backups:z
    env_file:
      - ../.envs/.local/.postgres
    ports:
      - "5432:5432"

  redis:
    image: redis:6
    container_name: napse_dtk_local_redis

  celeryworker:
    <<: *django
    image: napse_dtk_local_celeryworker
    container_name: napse_dtk_local_celeryworker
    depends_on:
      - redis
      - postgres
      - django
    ports: []
    command: /start-celeryworker
    labels:
      - "traefik.enable=false"

  celerybeat:
    <<: *django
    image: napse_dtk_local_celerybeat
    container_name: napse_dtk_local_celerybeat
    depends_on:
      - redis
      - postgres
      - django
    ports: []
    command: /start-celerybeat
    labels:
      - "traefik.enable=false"
